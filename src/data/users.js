// Los usuarios ahora se autentican contra MongoDB via /api/auth/login.
// Este archivo se mantiene solo por compatibilidad con imports existentes.

export const demoUsers = [];

export const findUserByEmail = () => null;
export const validateUser = () => null;
